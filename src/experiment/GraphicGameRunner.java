package experiment;

import controller.*;
import model.*;

public class GraphicGameRunner implements GameRunner {

    @Override
    public GameResult runGame(ExperimentConfig config) {

        // Plateau plateau = new Plateau(config.getGridSize(),config.getGridSize());

        // GameController controller = new GameController(plateau);

        // long start = System.currentTimeMillis();
        // int turns = controller.runGame(); 
        // long end = System.currentTimeMillis();

        // return new GameResult( controller.getWinningTeam(), end - start, turns);
        return null;
    }
}
